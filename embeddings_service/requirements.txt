fastapi==0.104.1
uvicorn[standard]==0.24.0
# NOTE: builder stage needs heavy deps (torch, transformers) to export ONNX.
# The runtime will only require onnxruntime and tokenizers/transformers.
# Keep this file minimal; the Dockerfile builder will install the heavy packages to export ONNX.

